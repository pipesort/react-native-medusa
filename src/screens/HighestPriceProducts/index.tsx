import React from 'react';

import Collection from '@app/components/Collection';

const HighestPriceProducts = ({ navigation, route }) => (
  <Collection navigation={navigation} route={route} />
);

export default HighestPriceProducts;
