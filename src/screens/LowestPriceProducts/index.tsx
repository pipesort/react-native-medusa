import React from 'react';

import Collection from '@app/components/Collection';

const LowestPriceProducts = ({ navigation, route }) => (
  <Collection navigation={navigation} route={route} />
);

export default LowestPriceProducts;
